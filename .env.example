# Supabase → Project Settings → API
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Server-only (set in Vercel → Settings → Environment Variables, NOT in this file):
# SUPABASE_SERVICE_ROLE_KEY=...   used by /api/invite-admin to send admin invitations
